# 클러스터 제출 규칙 (연습용 자리 — 팀 환경으로 교체할 것)

- 제출 명령: `bsub -q tcad -n 4 -o %J.log stcad run.cmd` *(예시 — 팀 큐 이름으로 교체)*
- 라이선스: 동시 실행 최대 8 스플릿. 초과분은 큐에 대기시킨다.
- 야간(20시 이후) 대량 스윕 권장, 주간에는 4 스플릿 이하.
- 결과 폴더 규칙: `/proj/<과제코드>/splits/<조건조합>/`
